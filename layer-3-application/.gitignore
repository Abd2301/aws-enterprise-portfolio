# Terraform state files - contain sensitive resource details
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Local tfvars with sensitive values (SSO profiles, etc)
# CI tfvars are safe to commit (no secrets)
terraform/environments/production.tfvars

# Lambda build artifacts
*.zip